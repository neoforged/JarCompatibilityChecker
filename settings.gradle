pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.neoforged.net/releases/' }
        mavenLocal()
    }
}

rootProject.name = 'JarCompatibilityChecker'
include(':tests:base')
include(':tests:top')
include(':plugin')
